
#ifndef SCHEME_CPP_SCHEME_HELPERS_HPP
#define SCHEME_CPP_SCHEME_HELPERS_HPP

typedef struct scheme scheme;
typedef struct cell * pointer;

namespace script
{
  pointer scNil(scheme * sc);
}

#endif

